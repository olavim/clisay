// Array get/set heavy: exercises NativeArray indexing (bounds/type checks).
say arr = mut [0, 0, 0, 0, 0, 0, 0, 0];
say var i = 0;
say var sum = 0;
while i < 300000 {
    say idx = i & 7;
    arr[idx] = idx;
    sum = sum + arr[idx];
    i = i + 1;
}
