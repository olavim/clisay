//! The back end: the concrete bytecode encoding (the `Chunk` format and its
//! `OpCode` instruction set) that the compile pipeline ultimately produces and
//! the interpreter executes.

pub mod bytecode;
