fn fib(n) {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}
fn elapsed(start, value) {
    print(value);
    print(clock() - start);
}
elapsed(clock(), fib(39));
// elapsed(clock(), 1);