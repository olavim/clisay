fn getCounter() {
    say n = 0;
    fn counter() {
        n += 1;
        return n;
    }
    return counter;
}

say counter = getCounter();
print(counter()); // 1
print(counter()); // 2
print(counter()); // 3
