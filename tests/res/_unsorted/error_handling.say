say array = [1, 2, 3, 4, 5];

fn getIndex(i) result {
    if i < 0 || i >= array.length() {
        error "Index out of bounds";
    }
    ok array[i];
}

say value = getIndex(0) error {
    print(error.message);
};