// An immutable field may be assigned only once, even within init.
type T {
    pub value;
    init() {
        this.value = 1;
        this.value = 2; // error: Immutable field `T.value` is initialized more than once
    }
}
