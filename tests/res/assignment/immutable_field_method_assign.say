// An immutable field cannot be reassigned from a method, even a public one.
type T {
    pub value;
    init(this) {
        this.value = 1;
    }
    fn reset(this) {
        this.value = 2; // error: Cannot assign immutable field `T.value`
    }
}
