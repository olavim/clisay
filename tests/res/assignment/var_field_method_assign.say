// A `var` field may be reassigned from a method.
type T {
    pub var value;
    init(this) {
        this.value = 1;
    }
    pub fn set(this) {
        this.value = 2;
    }
}
say t = T();
t.set();
print(t.value); // expect: 2
