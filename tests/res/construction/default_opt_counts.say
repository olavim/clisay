// A nullable field counts as defaulted for the synthesized init. `field?` and `field: opt` spell
// the same nullability.
type T {
    pub x = 5;
    pub y?;
    pub z: opt;
}
say t = T();
print(t.x); // expect: 5
print(t.y); // expect: null
print(t.z); // expect: null
