// A non-null private field with no default and no factory assignment is never initialized.
type T {
    pub x;
    y;
    init(this) { // error: Non-null field 'y' is never initialized
        this.x = 1;
    }
}
