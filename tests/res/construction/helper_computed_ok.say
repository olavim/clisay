// An init computes a field value inline from another field; `this.<field>` reads are allowed.
type A {
    knob = 10;
    pub value;
    init(n) {
        this.value = this.knob + n;
    }
}
say a = A(5);
print(a.value); // expect: 15
