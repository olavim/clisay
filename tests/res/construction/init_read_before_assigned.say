// Reading a non-null field before init assigns it is rejected.
type T {
    a;
    b;
    init() {
        this.a = this.b; // error: Field 'b' is used before it is assigned
        this.b = 5;
    }
}
