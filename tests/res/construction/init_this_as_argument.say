// Passing the raw `this` to a function would expose a half-built object.
fn keep(t?) {
}
type T {
    pub x;
    init() {
        this.x = 1;
        keep(this); // error: the partially-constructed 'this' is not a value in a factory
    }
}
