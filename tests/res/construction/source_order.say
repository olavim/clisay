// A brace evaluates its fields left to right.
fn a()! { print("a"); return 1; }
fn b()! { print("b"); return 2; }
type T { pub m; pub n; }
say t = T { m: a(), n: b() };
// expect: a
// expect: b
print(t.m); // expect: 1
print(t.n); // expect: 2
