type Point { pub x; pub y; init(this, *x, *y) { this.x = x; this.y = y; } }
fn show(p) { say { x, y } = p; print(x); } // error: a refutable `say` pattern statement must have an `else` branch
show(Point(1, 2));
