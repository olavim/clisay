fn f(n)! {
  if n == 0 { return 1 + true; }
  return f(n - 1);
}
f(2);
// error(full):
// error: Operator '+' cannot be applied to operands number and boolean
//  --> frame_recursion.say:3:26
//   |
// 2 | fn f(n)! {
// 3 |   if n == 0 { return 1 + true; }
//   |                          ^^^^
// 	at f (frame_recursion.say:3)
// 	at tail calls
// 	  f (frame_recursion.say:3)
// 	at script (frame_recursion.say:6)
