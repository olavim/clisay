type Holder {
  pub var data;
  init(this) { this.data = mut []; }
  pub fn leak(this): mut { return this.data; }
}
// error(full):
// error: Cannot return the mutable field 'data'; it would move out of the receiver
//  --> help_return_mutable_field.say:4:35
//   |
// 3 |   init(this) { this.data = mut []; }
// 4 |   pub fn leak(this): mut { return this.data; }
//   |                                   ^^^^^^^^^
//   = help: freeze or copy it before returning
