trait Sink {
    req fn accept(this, x: *mut);
}
type Db with Sink {
    pub fn accept(this, x: mut) { }
    init(this) {}
}
// error(full):
// error: parameter is less permissive than the trait requires
//  --> help_trait_param_not_move.say:5:25
//   |
// 2 |     req fn accept(this, x: *mut);
//   |                         ------- `Sink.accept` requires ownership of `x` (`*mut`)
// ...
// 5 |     pub fn accept(this, x: mut) { }
//   |                         ^^^^^^ `Db.accept` only borrows `x` here (`mut`)
//   |
//   = help: take ownership of `x` to match the trait, by declaring it `*mut`
