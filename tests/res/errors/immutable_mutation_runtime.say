fn mutate(x) { x["k"] = 1; }
say m = mut {};
freeze(m);
mutate(m);
// error(full):
// error: cannot mutate an immutable value
//  --> immutable_mutation_runtime.say:1:16
//   |
// 1 | fn mutate(x) { x["k"] = 1; }
//   |                ^ `x` is immutable
//   |
// 	at mutate (immutable_mutation_runtime.say:1)
