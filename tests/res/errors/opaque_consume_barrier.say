fn consumes(x: *mut) { }
fn apply(cb, val: mut) {
  cb(val);
}
say v = mut {};
apply(consumes, v);
// error(full):
// error: cannot move a borrowed argument into a consuming callee
//  --> opaque_consume_barrier.say:3:6
//   |
// 2 | fn apply(cb, val: mut) {
// 3 |   cb(val);
//   |      ^^^ the callee consumes `val`
//   |
// 	at apply (opaque_consume_barrier.say:3)
