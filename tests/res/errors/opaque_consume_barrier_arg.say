fn cb(a: mut, b: *mut) { }
fn apply(f, x: mut, y: mut) {
  f(x, y);
}
say p = mut {};
say q = mut {};
apply(cb, p, q);
// error(full):
// error: cannot pass a borrowed argument to a callee that retains it
//  --> opaque_consume_barrier_arg.say:3:8
//   |
// 2 | fn apply(f, x: mut, y: mut) {
// 3 |   f(x, y);
//   |        ^ `y` is borrowed but the callee retains it
//   |
// 	at apply (opaque_consume_barrier_arg.say:3)
// 	at script (opaque_consume_barrier_arg.say:7)
