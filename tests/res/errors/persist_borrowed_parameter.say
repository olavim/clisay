fn stash(bin: mut, item) { bin.push(item); }
say sink = mut [];
say m = mut [1, 2];
stash(sink, m);
// error(full):
// error: cannot retain a borrowed argument
//  --> persist_borrowed_parameter.say:1:37
//   |
// 1 | fn stash(bin: mut, item) { bin.push(item); }
//   |                    ----             ^^^^ `item` is retained here
//   |                    |
//   |                    `item` is borrowed here
//   |
//   = help: declare it `*item` to retain it, or freeze or copy it before persisting
