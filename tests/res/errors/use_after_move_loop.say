fn consume(*mut x) {}
say var n = 0;
say a = mut [1];
while n < 3 {
  consume(a);
  n = n + 1;
}
// error(full):
// error: cannot give away `a`, which no longer writes its value
//  --> use_after_move_loop.say:5:11
//   |
// 4 | while n < 3 {
// 5 |   consume(a);
//   |           ^^ `a` gave its write-ownership here
//   |           |
//   |           `a` is given away here
