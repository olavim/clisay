type Foo {
    pub val;
    init(this, v) {
        this.val = v;
    }
    pub fn bar(this) {
        print(this.val);
    }
}
say m = Foo("h" + "i").bar;
gcCollect();
say a = Foo("xx" + "yy");
say b = Foo("zz" + "ww");
say c = Foo("pa" + "dd");
m(); // expect: hi
