gcStress(false);
type Node {
    pub next?;
    init(this, n?) {
        this.next = n;
    }
}
say var head? = null;
say var i = 0;
while i < 50000 {
    head = Node(head);
    i = i + 1;
}
gcCollect();
say var count = 0;
say var cur? = head;
while cur != null {
    count = count + 1;
    cur = cur.next;
}
print(count); // expect: 50000
