// A string literal is interned, so the argument and a later literal are one object.
say sink = mut [];
fn give(s) { sink.push("hello"); }
give("hello");
print(sink[0]); // expect: hello

// @split
say sink = mut [];
fn give(s) { sink.push(s); } // error: cannot retain a borrowed argument
give("hello");
