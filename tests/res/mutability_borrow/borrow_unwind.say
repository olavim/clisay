// A borrow marked for a call that throws is restored on the unwind, so the value is usable after.
say sink = mut [];
fn boom(x) { throw "e"; }
say m = mut [1, 2];
try { boom(m); } catch e { }
sink.push(m);
print(sink[0][0]); // expect: 1
