// A borrow forwarded to an opaque callback that persists it into a brace construction traps at runtime.
type Wrap { pub w?; init() { } }
say mut kept? = null;
fn relay(x, cb) { cb(x); }
say m = mut [1, 2];
relay(m, v => kept = Wrap { w: v }); // error: cannot persist a borrowed value
