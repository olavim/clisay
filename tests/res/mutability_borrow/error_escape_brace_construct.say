type Wrap { pub w?; init(this) { } }
say var kept? = null;
fn relay(x, cb) { cb(x); } // error: cannot pass a borrowed argument to a callee that retains it
say m = mut [1, 2];
relay(m, v => kept = Wrap { w: v });
