// A `no persist` parameter's borrow forwarded to a persisting callback traps at the store site.
obligation borrowed: no persist;
say sink = mut [];
fn keeper(v) { sink.push(v); } // error: cannot persist a borrowed value
fn each(xs: borrowed, cb) { cb(xs); }
say m = mut [1, 2];
each(m, keeper);
