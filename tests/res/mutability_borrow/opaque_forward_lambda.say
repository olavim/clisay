// A `no persist` parameter may forward its borrow to a borrow-only lambda callback.
obligation borrowed: no persist;
fn each(xs: borrowed, cb) { cb(xs); }
say m = mut [1, 2];
each(m, v => print(v[0])); // expect: 1
