// A method handing back its receiver gives the new name the write-ownership. The old one reads.
type C {
    pub var v;
    init(this) { this.v = 0; }
    pub fn set(this: mut, n)! { this.v = n; return this; }
}
say a = mut C();
say b = a.set(1);
print(a.v); // expect: 1
