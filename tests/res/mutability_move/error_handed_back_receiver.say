// A method handing back its receiver is the same second name, so binding it moves too.
type C {
    pub mut v;
    init(this) { this.v = 0; }
    pub fn set(this: mut, n)! { this.v = n; return this; }
}
say a = mut C();
say b = a.set(1);
print(a.v); // error: value used after it was moved
