// A value given away before a loop is still readable, whether or not a rebind inside the loop runs.
fn consume(*mut x) {}
say var a = mut [1];
consume(a);
say cond = false;
while cond {
  a = mut [2];
}
print(a[0]); // expect: 1
