// Returning a mutable field would move it out of the borrowed receiver; freeze or copy instead.
type Holder {
  pub var data;
  init(this) { this.data = mut []; }
  pub fn leak(this): mut { return this.data; } // error: Cannot return the mutable field 'data'; it would move out of the receiver
}
