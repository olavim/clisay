// A `mut` parameter is a borrow, so persisting its argument is refused where the body writes it.
say sink = mut [];
fn borrows(x: mut) { sink.push(x); } // error: a `mut` parameter borrows its argument and cannot let it escape
say m = mut [1, 2];
borrows(m);
