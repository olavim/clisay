// The demand survives a branch join: consumed on one path and read after, so the read still fails.
type Box { pub mut v; init(this) { this.v = 0; } }
fn eats(x: *mut) { }
say consume = eats;
say a = mut Box();
if (1 > 0) {
    consume(a);
}
a.v = 1; // error: value used after a call consumed it
