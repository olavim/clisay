// A bracket write fails when the callee turned out to retain the argument.
fn eats(*mut x) { }
say consume = eats;
say a = mut [0];
consume(a);
a[0] = 1; // error: cannot write a value whose write-ownership was given away
