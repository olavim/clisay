// The opaque-call barrier reaches a consuming `*mut` parameter through a bound method too.
type Box { pub fn eat(x: *mut) { } }
fn apply(cb, val: mut) {
  cb(val); // error: cannot move a borrowed argument into a consuming callee
}
say b = Box();
say v = mut {};
apply(b.eat, v);
