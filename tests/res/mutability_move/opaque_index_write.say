// A bracket write demands the same runtime proof a read does, and a borrowing callee passes it.
fn borrows(mut x) { x[0] = x[0] + 1; }
say lend = borrows;
say a = mut [0];
lend(a);
a[0] = 5;
print(a[0]); // expect: 5
