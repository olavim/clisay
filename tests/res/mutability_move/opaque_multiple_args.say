type Box { pub var v; init(this) { this.v = 0; } }
fn eats(mut x) { }
say pair = (x, y) => 0;
say consume = eats;
say kept = mut Box();
say given = mut Box();
say r = pair(given, kept);
kept.v = 7;
print(kept.v);   // expect: 7
say other = mut Box();
consume(other);
print("ok");     // expect: ok
