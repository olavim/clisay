// Rebinding gives the slot a value of its own, so the earlier call has nothing left to prove.
type Box { pub mut v; init(this) { this.v = 0; } }
fn eats(x: *mut) { }
say consume = eats;
say mut a = mut Box();
consume(a);
a = mut Box();
a.v = 1;
print(a.v); // expect: 1
