// An untagged return auto-freezes the mutable it hands back, so the caller gets an immutable value.
fn inner(): mut { return mut {}; }
fn make()! { return inner(); }
say r = make();
r["k"] = 1; // error: 'r' is immutable and cannot be mutated
