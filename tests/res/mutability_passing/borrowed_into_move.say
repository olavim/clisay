// A borrowed `mut` parameter cannot be moved into a consuming `*mut` parameter on a direct
// call: the caller only lent the value and cannot give it away.
fn sink(xs: *mut) { }
fn launder(ys: mut) {
  sink(ys); // error: cannot move borrowed value `ys`
}
