// A `: mut` factory returning `mut Ctor()` carries the constructed type's tag, so a method call on
// its result still runs the callee's argument checks.
type Foo { pub fn take(x) { } }
fn make(): mut { return mut Foo(); }
make().take(null); // error: expected non-null argument
