// A `mut` parameter borrows the caller's value and fills it in place.
fn addDefaults(mut config) {
  config["timeout"] = 30;
}
say cfg = mut {};
addDefaults(cfg);
print(cfg["timeout"]); // expect: 30
