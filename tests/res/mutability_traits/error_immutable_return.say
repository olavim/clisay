// A `: mut` satisfier of an immutable hole would leak a runtime-mutable value through an immutable slot.
trait Source {
    req fn read(this);
}
type File with Source {
    pub fn read(this): mut { return mut []; } // error: return is mutable but trait requires immutable
    init(this) {}
}
print("unreachable");
