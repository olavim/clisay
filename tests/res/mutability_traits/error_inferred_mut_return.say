// Trait conformance depends on the satisfier's inferred return capability, not just an explicit tag.
trait Source {
    req fn read(this): opt;
}
type File with Source {
    pub fn read(this) { return mut []; } // error: return is mutable but trait requires immutable
    init(this) {}
}
print("unreachable");
