// A `*mut` hole cannot be satisfied by a borrow.
trait Sink {
    req fn accept(this, x: *mut);
}
type Db with Sink {
    pub fn accept(this, x: mut) { } // error: parameter is less permissive than the trait requires
    init(this) {}
}
print("unreachable");
