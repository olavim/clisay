// A `*mut` hole cannot be satisfied by a borrow.
trait Sink {
    req fn accept(this, *mut x);
}
type Db with Sink {
    pub fn accept(this, mut x) { } // error: parameter is less permissive than the trait requires
    init(this) {}
}
print("unreachable");
