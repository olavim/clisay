// A `*mut` hole is satisfied by a `*mut` param.
trait Sink {
    req fn accept(this, *mut x);
}
type Db with Sink {
    pub fn accept(this, *mut x) { }
    init(this) {}
}
say d = Db();
print("ok"); // expect: ok
