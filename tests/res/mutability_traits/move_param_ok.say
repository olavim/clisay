// A `*mut` hole is satisfied by a `*mut` param.
trait Sink {
    req fn accept(this, x: *mut);
}
type Db with Sink {
    pub fn accept(this, x: *mut) { }
    init(this) {}
}
say d = Db();
print("ok"); // expect: ok
