// A `: mut` `req fn` return is satisfied by a `: mut` method.
trait Source {
    req fn read(this): mut;
}
type File with Source {
    pub fn read(this): mut { return mut []; }
    init(this) {}
}
say f = File();
print("ok"); // expect: ok
