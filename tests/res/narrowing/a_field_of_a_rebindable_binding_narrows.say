type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    if (b.value == null) { return; }
    say y = b.value;
    print(y); // expect: v
}
run();

// @split
type Box { pub value: opt; }
fn run() {
    say b = Box { value: "v" };
    if (b.value == null) { return; }
    say y = b.value;
    print(y); // expect: v
}
run();

// @split
type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    if (b.value == null) { return; }
    b = Box { value: null };
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run();

// @split
type Box { pub value: opt; }
fn run(k) {
    say var b = Box { value: "v" };
    if (b.value == null) { return; }
    if (k == 1) { b = Box { value: null }; }
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run(1);
