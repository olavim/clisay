type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    fn wipe() { b = Box { value: null }; }
    fn go() { wipe(); }
    if (b.value == null) { return; }
    go();
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run();

// @split
type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    fn go() { say f = () => { b = Box { value: null }; return 1; }; f(); }
    if (b.value == null) { return; }
    go();
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run();

// @split
type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    fn go() { fn wipe() { b = Box { value: null }; } wipe(); }
    if (b.value == null) { return; }
    go();
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run();

// @split
type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    fn go() { fn helper() { say b = 1; print(b); } b = Box { value: null }; }
    if (b.value == null) { return; }
    go();
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run();

// @split
type Box { pub value: opt; }
fn run() {
    say var b = Box { value: "v" };
    say f = () => { if (1 == 2) { say b = 1; print(b); } b = Box { value: null }; return 1; };
    if (b.value == null) { return; }
    f();
    say y = b.value; // error: Cannot assign a nullable value to non-null binding 'y'
    print(y);
}
run();

// @split
type Box { pub value: opt; }
fn count()! { say var n = 0; n = n + 1; return n; }
fn run() {
    say var b = Box { value: "v" };
    if (b.value == null) { return; }
    count();
    say y = b.value;
    print(y); // expect: v
}
run();

// @split
type T { pub value: opt; }
fn run() {
    say var x = T { value: "v" };
    if (x.value == null) { return; }
    say trippy = () => { say var x = 0; x = 1; return x; };
    trippy();
    say y = x.value;
    print(y); // expect: v
}
run();
