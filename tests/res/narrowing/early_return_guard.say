// An early return on the null branch narrows the value non-null on the path that falls through.
fn first(x: opt)! {
  if x == null { return 0; }
  return x;
}
print(first(5)); // expect: 5
print(first(null)); // expect: 0
