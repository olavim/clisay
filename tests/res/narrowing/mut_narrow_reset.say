// Reassigning a `mut` binding resets the narrowing established by the guard.
fn find()? {
    return null;
}
say mut h?;
if (h != null) {
    h = find();
    h.x; // error: unchecked value owes 'opt'
}
