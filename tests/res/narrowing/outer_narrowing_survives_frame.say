// Checking a nested body leaves the enclosing frame's narrowings intact, so `t.x` is still narrowed
// after the nested type closes.
type T {
    pub x: opt;
    init(a: opt) { this.x = a; }
    pub fn run() {
        say t = this;
        if (t.x == null) { return; }
        type K {
            pub fn runK() { print("k"); }
        }
        K().runK();     // expect: k
        print(t.x + 1); // expect: 6
    }
}
T(5).run();
