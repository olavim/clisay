// A legitimate Err crossing the dynamic boundary into a `fails` slot lands there. `??` discharges it.
say make = () => Err("boom");
say r: fails = make();
print(r ?? "recovered"); // expect: recovered
