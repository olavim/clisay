// Pushing a pending value onto a native array makes the array carry the obligation.
type Taint { } obligation taint { witness Taint; discharge to use; }
say t: taint = "x";
say a: [taint] = [];
a.push(t);
print(a.length()); // expect: 1
