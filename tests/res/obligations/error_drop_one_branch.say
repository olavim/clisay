// A debt is settled only if every path settles it.
fn fail(): fails { return Err("boom"); }
fn f(c) {
    say r: fails = fail(); // error: 'r' owes 'fails' and is never discharged
    if c { r ?? 0; }
}
