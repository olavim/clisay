// A lone catch-all arm rules nothing out, so it settles nothing. The arm binds nothing, so the
// scrutinee is the only binding left owing.
fn f(): opt fails { return Err("b"); }
fn g() {
    say x: opt fails = f(); // error: 'x' owes 'fails' and is never discharged
    match x { _ => 1 }
}
