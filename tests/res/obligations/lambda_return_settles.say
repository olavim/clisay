fn f(): fails { return Err("b"); }

say hands = () => {
    say x: fails = f();
    return x;
};

say drops = () => {
    say y: fails = f(); // error: 'y' owes 'fails' and is never used
    return 1;
};
