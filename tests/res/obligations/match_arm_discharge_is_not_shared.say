fn f(): fails { return Err("e"); }
fn run(k): void {
    say x: fails = f();
    match k {
        1 => { if (x ~ Err) { return; } },
        2 => { print("two"); },
        _ => { if (x ~ Err) { return; } }
    }
    say y = x; // error: cannot assign a value owing 'fails' to 'y'
}
run(2);
