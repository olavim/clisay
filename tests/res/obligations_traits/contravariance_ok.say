// A satisfier may accept more obligations than the hole passes, since it guards against them anyway.
trait Sink {
    req fn accept(x: opt);
}
type Db with Sink {
    pub fn accept(x: opt fails) { }
    init() {}
}
say d = Db();
print("ok"); // expect: ok
