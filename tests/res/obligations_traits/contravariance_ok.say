// A satisfier may accept more obligations than the hole passes, since it guards against them anyway.
trait Sink {
    req fn accept(this, x: opt);
}
type Db with Sink {
    pub fn accept(this, x: opt fails) { x ?? 0; }
    init(this) {}
}
say d = Db();
print("ok"); // expect: ok
