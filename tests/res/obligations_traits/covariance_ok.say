// A `req fn` owing `fails` is satisfied by a clean read, since a clean return owes less.
trait Source {
    req fn read(this): fails;
}
type File with Source {
    pub fn read(this)! { return 1; }
    init(this) {}
}
say f = File();
print(f.read()); // expect: 1
