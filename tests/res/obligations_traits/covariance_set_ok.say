// A `req fn` owing a set is satisfied by a return promising a subset of it.
trait Source {
    req fn read(this): opt fails;
}
type File with Source {
    pub fn read(this)? { return null; }
    init(this) {}
}
say f = File();
print("ok"); // expect: ok
