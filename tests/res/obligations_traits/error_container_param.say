// Container shape is checked per parameter, not just on the return.
trait Sink {
    req fn accept(x: [fails]);
}
type Db with Sink {
    pub fn accept(x: fails) { } // error: Method 'accept' satisfies 'req fn accept' but parameter 1 is not a container, which the requirement declares
    init() {}
}
print("unreachable");
