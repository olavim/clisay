// The reverse also fails: a container return does not satisfy a hole expecting a bare value.
trait Source {
    req fn read(): fails;
}
type File with Source {
    pub fn read(): [fails] { return [Err("x")]; } // error: return shape does not match the trait
    init() {}
}
print("unreachable");
