// Contravariance is checked per parameter; the diagnostic names the offending position.
trait Sink {
    req fn accept(a, b: opt);
}
type Db with Sink {
    pub fn accept(a, b) { } // error: parameter rejects an obligation the trait passes
    init() {}
}
print("unreachable");
