// A satisfier's inferred return obligation is caught, not just a declared one.
fn risky(): fails { return Err("x"); }
trait Loader {
    req fn load();
}
type App with Loader {
    pub fn load() { return risky()?!; } // error: return owes an obligation the trait forbids
    init() {}
}
print("unreachable");
