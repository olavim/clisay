// The `?` marker and the `: opt` clause name the same obligation across the require/satisfy seam.
trait Sink {
    req fn accept(x?);
}
type Db with Sink {
    pub fn accept(x: opt) { }
    init() {}
}
say d = Db();
print("ok"); // expect: ok
