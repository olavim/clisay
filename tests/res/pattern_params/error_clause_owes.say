// The clause rides the slot the pattern names, so an `@` binder still owes it.
obligation borrowed: no persist;
type Box { pub item; init(this, i) { this.item = i; } }
fn forward(x @ Box : borrowed) {
    return x; // error: 'x' owes 'borrowed'; it cannot be stored or escape its scope
}
