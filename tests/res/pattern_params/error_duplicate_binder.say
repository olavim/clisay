// A pattern's binders are parameter names, so they collide with a later parameter.
type Point { pub x; pub y; init(this, a, b) { this.x = a; this.y = b; } }
fn f(p @ Point { x, y }, x) { print(x); } // error: 'x' already declared in this scope
f(Point(1, 2), 9);
