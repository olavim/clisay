// A nested block may redeclare a name from an enclosing scope (shadowing is legal;
// only same-scope duplicates collide).
say x = 1;
{
  say x = 2;
  print(x); // expect: 2
}
print(x); // expect: 1
