type A {
    prop;
    value;

    set(prop, value) {
        this.prop = prop;
        this.value = value;
    }
}

type B : A { }

say obj = B();
obj.foo = "bar";
print(obj.prop); // expect: foo
print(obj.value); // expect: bar