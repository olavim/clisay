type A {
    pub x = "A";
    pub fn getX() {
        return this.x;
    }
}

type B : A {
    pub x = "B";
    pub fn getSuperX() {
        return super.getX();
    }
}

say obj = B();
print(obj.getSuperX()); // expect: A