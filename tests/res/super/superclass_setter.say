type A {
    pub value;

    set(prop, value) {
        this.value = prop + "." + value;
    }
}

type B : A {
    pub fn set(a, b) {
        super[a] = b;
    }
}

say obj = B();
obj.set("foo", "bar");
print(obj.value); // expect: foo.bar