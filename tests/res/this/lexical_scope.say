type A {
    pub x;
    init(this, v) {
        this.x = v;
    }
    pub fn withParam(this, x)! {
        return this.x + "/" + x;
    }
}
say a = A("member");
print(a.withParam("param")); // expect: member/param
print(a.x);                  // expect: member
