type A {
    pub x;

    pub fn a() {
        print(this.x);
    }
}

type B : A {
    pub x;

    pub fn b() {
        print(this.x);
    }
}

say obj = B();
obj.x = "b";
obj.a(); // expect: null
obj.b(); // expect: b