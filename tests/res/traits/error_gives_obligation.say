// A `gives` delegate must be a plain, non-null field: a null delegate provides no trait.
trait Powered { pub fn go(this)! { return "v"; } }
type Engine with Powered { init(this) {} }
type Car {
    pub engine? gives Powered; // error: `gives` delegate field 'engine' cannot carry obligations
}
