// A `pub`/`inner` (exposed) member and a same-named private member in a different trait do not clash.
trait Greeter {
    pub fn greet(this)! { return "hello"; }
}
trait Secret {
    fn greet(this)! { return "shh"; } // private; distinct from Greeter.greet
    pub fn reveal(this)! { return this.greet(); } // calls its own private greet
}

type Person with Greeter, Secret {}
say p = Person();
print(p.greet());   // expect: hello
print(p.reveal());  // expect: shh
