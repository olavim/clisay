// Inside a method, `this["x"]` resolves to the same member as `this.x` (statically).
type Counter {
    pub var n = 0;

    pub fn bump(mut this) {
        this["n"] = this["n"] + 1;
    }
}

say c = mut Counter();
c.bump();
c.bump();
print(c.n); // expect: 2
