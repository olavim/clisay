type Widget {
    inner state;
    pub fn read(this)! { return this.state; } // internal: ok
    init(this, s) { this.state = s; }
}
say w = Widget("ready");
print(w.read()); // expect: ready
print(w.state); // error: Invalid index: Widget doesn't have member "state"
